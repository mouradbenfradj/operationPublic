<form method="post" action="{{ path('app_suivit_delete', {'id': suivit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ suivit.id) }}">
    <button class="btn">Delete</button>
</form>
